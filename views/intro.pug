doctype html
html(lang='ko')
  head
    title My Real Trekking
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css')
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js')
    script(src='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js')
    style.
      body {
      margin-top: 100px;
      margin-bottom: 100px;
      margin-right: 150px;
      margin-left: 80px;
      }
    
body
  .container
  h1 원하는 조건들을 넣으세요
  p 당신에게 꼭 맞는 트레킹 코스를 추천합니다.
  br
  form(action='/return', method='post')
     label 난 이 거리 이상은 도저히 갈 수 가 없다!!!!
       br
       input(type='text' name='course_distance' placeholder='distance')
     br
     br
     label 내 체력을(1)로 봤을때 이 이상은 무리다!!! 
       br
       input(type='text' name='course_difficulty' placeholder='difficulty')
     br
     br
     br
     p
       input(type='submit' value='submit')